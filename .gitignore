# 음원은 저장소에 넣지 않는다.
# 배경음은 공식 MV 를 유튜브로 재생하므로(js/data.js 의 BGM) 이 파일은 더 이상 쓰이지 않는다.
*.mp3
*.bak

# 멤버 프로필 사진 — 소속사 자료라 재배포하지 않는다.
# 없으면 이름만 뜨고 나머지는 그대로 돈다.
assets/members/

node_modules/
tools/.tmp/
tools/three.module.js
