# 이 저장소가 담고 있는 것 / 담지 않는 것

## MIT 가 적용되는 범위

`LICENSE`(MIT)는 **이 저장소의 코드**에만 적용됩니다 —
`js/`, `css/`, `index.html`, `tools/`.

## 저작권이 제3자에게 있는 것

이 프로젝트는 리센느(RESCENE)라는 실존 그룹에 대한 **비영리 팬 시각화**입니다.
아래는 저작권이 권리자에게 있으며, 이 저장소는 그것을 **담지 않습니다.**

| | 어떻게 다루나 |
| --- | --- |
| **음원** | 저장소에 넣지 않습니다. 배경음은 공식 MV 를 **유튜브 IFrame API 로 재생**합니다 (`js/data.js` 의 `MVS`). 유튜브 약관에 따라 플레이어를 숨기지 않고 200×200 이상으로 표시합니다. |
| **영상** | 임베드만 합니다 (`youtube-nocookie.com`). 다운로드·재호스팅하지 않습니다. |
| **썸네일** | `i.ytimg.com` 에서 그때그때 불러옵니다. 복사본을 두지 않습니다. |
| **멤버 사진** | `assets/members/` 는 `.gitignore` 에 있습니다. 소속사 자료라 재배포하지 않습니다. 파일이 없으면 사진 자리가 비고 이름만 표시되며, 나머지 기능은 그대로 동작합니다. |
| **글꼴** | IBM Plex (구글 폰트, SIL Open Font License) — CDN 에서 불러옵니다. |
| **three.js** | r169, MIT — CDN 에서 불러옵니다. |

## 데이터에 대하여

`js/data.js` 의 날짜·조회수·제목은 공개된 자료(유튜브 메타데이터, 언론 기사,
나무위키, 커뮤니티 게시물)에서 모은 **사실 정보**입니다.
확인되지 않은 항목은 코드 주석에 `추정` 으로 표시해 두었습니다.

멤버 소개에는 본명·출신·별명과, **본인이 방송에서 직접 밝혀 이미 널리 알려진**
가족 관련 일화가 들어 있습니다. 성함이 공개되지 않은 분은 역할만 적었습니다.
당사자나 소속사에서 삭제를 원하시면 이슈로 알려주세요. 바로 지우겠습니다.

## 요청

권리자이시고 이 저장소의 내용 중 문제가 되는 부분이 있다면
GitHub 이슈로 알려주시면 즉시 조치하겠습니다.
